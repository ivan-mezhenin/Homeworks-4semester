\documentclass[12pt]{article}
\usepackage{graphicx} % Required for inserting images




\begin{document}

$S K K = (\lambda xyz. x~z~(y~z))~(\lambda xy.x)~(\lambda xy.x) = (\lambda yz. K~z~(y~z)) ~K = \lambda z. K~z~(K~z) = \lambda z. (\lambda xy.x)~z~(\lambda xy.x)~z = \lambda z. (\lambda y.z) ~(\lambda y.z) = \lambda z.z = I$

\end{document}
